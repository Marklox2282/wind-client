export type BootAppearance = {
  cloudAccentColor: string;
  loadingScreenTitle: string;
  loadingScreenTagline: string;
};

export const DEFAULT_BOOT_APPEARANCE: BootAppearance = {
  cloudAccentColor: '#8ba4cf',
  loadingScreenTitle: 'Wind Client',
  loadingScreenTagline: 'Loading',
};
